{% extends 'base.html.twig' %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(userForm) }}
    {{form_rest(userForm)}}
    <button type="submit" class="btn btn-primary">Submit</button>
    {{form_end(userForm)}}
{% endblock %}
