{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des Catégories</h1>

    <table class="table table-dark table-striped" >
        <thead>
            <tr>
                
                <th>nom</th>
                <th>Options</th>
            </tr>
        </thead>
        <tbody>
         {% for competence in competences %}

                <tr>
                    
                    <td>{{ competence.categorie.categories }} </td>
                    <td>
                        <a class="btn btn-primary"  href="{{ path('competence_one_competence', { 'competences': competence.id }) }}">go</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}